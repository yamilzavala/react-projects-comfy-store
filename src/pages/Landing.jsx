import React from 'react';
import {FeaturedProducts, Hero} from '../components';

const Landing = () => {
    return (
        <div>
           <Hero/> 
           <FeaturedProducts/>
        </div>
    );
};

export default Landing;